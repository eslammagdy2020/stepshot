# Taste
- Uses the `/to-spec` workflow to turn an architecture review into an actionable implementation specification for the proposed changes. Confidence: 0.8
- Runs a two-axis review on changes before considering a phase/feature done: (1) a clean-code checklist (naming, functions, comments, SOLID, DRY/KISS/YAGNI) and (2) a list of AI-specific failure modes (plausible-but-wrong code, dead code, unreachable guards, convention divergence). Both axes are walked and findings are cited with file:line evidence. Confidence: 0.9
- Classifies review findings by severity (Critical / Important / Nits) and always proposes a specific fix alongside each finding. Critical findings must be fixed before a phase is closed; nits may be batched. Confidence: 0.9
- Insists on spec-mandated manual verification (e.g. real-hardware smoke checks) being performed and recorded in the same place as automated test results — not just "the tests pass". Confidence: 0.85
- Prefers unifying divergent code paths into one correct implementation over maintaining two side-by-side correct ones. When a fast path and a slow path disagree on a convention, delete the fast path rather than fix it. Confidence: 0.85
- Treats dead-code removal as part of the feature work that orphaned it, not as separate cleanup. Greps the repo to confirm zero callers before deleting. Confidence: 0.85
- Values deleting no-op defensive guards (e.g. `setDevicePixelRatio(1.0)` on a freshly-constructed `QPixmap`) and unreachable branches rather than leaving them "just in case". Confidence: 0.8
- Prefers extracting helper functions to shrink oversized functions (target ~10 lines of orchestration for the main entry point; per-concern helpers carry the rest). Confidence: 0.8
- Prefers mock tests that pin the exact capture/protocol contract (asserting on call tuples with positional args, not just output dimensions) — so the tests catch convention drift, not just behaviour drift. Confidence: 0.9
- Writes failure-path tests with explicit `pytest.raises(RuntimeError, match=...)` for every new error branch added by a feature. Confidence: 0.8
- When the repo has no commit history (zero commits), reconstructs a "before" snapshot of the changed files in a scratchpad directory from session context, then uses `git diff --no-index` to produce a real unified diff for review. Confidence: 0.85
- Always checks `git rev-parse HEAD; git log --oneline -5; git status --short` at the start of a review to confirm there is a fixed diff base. Confidence: 0.8
- Prefers structured findings reports with: Summary, Critical findings, Important findings, Nits, What's good, Self-check coverage (checklist of axes walked), Bottom line (counts + highest-value change). Confidence: 0.85
- Cites each finding with `file:line` and quotes the offending code, rather than describing it in prose. Confidence: 0.9
- Works on Windows where the shell is cmd-compatible: Unix tools like `head` are not available in pipelines — chain commands with `&` and use `powershell -NoProfile -Command` (Get-ChildItem / Sort-Object LastWriteTime) for file scans and sorting. Confidence: 0.8
- When git history can't yield a diff (zero-commit repos), pins the change set by file modification time (recent mtimes vs. earlier-phase dates) and reviews that file set directly, stating the pinning method in the review output. Complements the reconstruct-snapshot-and-diff approach. Confidence: 0.7
- Gives terse, phase-numbered task prompts (e.g. "review the latest code changes for phase 12", "start phase 14") with no pointers, expecting the agent to resolve context itself: spec section from the phased plan file (upgrade.md), standards from AGENTS.md, and the changed files pinned — without asking clarifying questions. "start phase N" means execute the whole phase autonomously end-to-end (spec → implement → test → log → summary). Confidence: 0.7
- Submits precise technical critiques with file:line citations and library-doc evidence (e.g. "Qt 6 docs specify grabWindow offset/size in device-independent pixels… L69–74, then squeezes back L83–89"), expecting the model to verify against docs before agreeing rather than deferring. Critiques arrive with reproductions already run (e.g. actual vs expected scene endpoint differing by exactly `pos()`) and a prescribed fix per finding. Confidence: 0.9
- Includes quantitative measured reproductions in critiques — not just "I ran it and it broke" but specific numerical evidence (e.g. "zoom 0.05 → handle is 200 px on screen; zoom 0.5 → 20 px; zoom 1.0 → 10 px; zoom ≥ ~1.7 → floored to 6 px") so the bug's severity and direction are provable from the report alone. Confidence: 0.85
- When re-reviewing after a "done"/"all phases complete" signal, re-reads the code and re-runs the empirical checks to confirm previously reported findings were actually fixed (reporting them as "still present" when not), rather than trusting the completion signal. Confidence: 0.85
- Confirms follow-up actions with one-word replies ("yes") when the proposed plan and reasoning are already in front of them — no restating of the plan needed. Confidence: 0.8
- Expects the model to trace downstream consumers of a changed contract before claiming "the fix is correct" — e.g. checking that `load_screenshot` / `addPixmap` / `boundingRect` / scene-rect math still hold after an output-size or DPR policy change. Confidence: 0.85
- Expects the model to explicitly call out when a "refactor" actually changed observable behaviour (vs just moving code), and to restore the original contract when that's the right call — rather than treating the refactor as a fait accompli. Confidence: 0.8
- Even after a fix lands, the spec-mandated manual verification gate (real-hardware smoke check) stays open in the same place as the automated-test summary until it is actually performed — not folded into "all green". Confidence: 0.85
- Runs a parallel-session implement/review split: one session writes the code while this session reviews it from here. Review requests may reference work produced in a different session, so the source of truth is the files saved to disk (not the other session's conversation, and not unsaved editor buffers), and review starts once the user signals the work has reached a reviewable state. Confidence: 0.6
ts while implementing, then run the full suite (+ acceptance-marked tests) after, and close by updating the phase status and "Verified green" counts in upgrade.md plus a short end-of-phase summary. Confidence: 0.7
- When new tests fail, root-causes each failure first and fixes the wrong side: corrects test assertions to pin real Qt semantics (compare `pixmap().cacheKey()` since `pixmap()` returns a copy; assert scene-space position via `start_point + pos()` after `moveBy`) and allows ±1 px tolerance where synthesized mouse events introduce rounding — rather than weakening the tests or bending the implementation. Confidence: 0.7
- Invalid input must be a true no-op: gate side effects (undo-state push, `image_changed` emit, status updates) on an explicit `bool` success return, and order side-effect calls after rejection guards so a rejected update leaves no half-applied state (e.g. `prepareGeometryChange()` moved after the min-length guard in `set_geometry`). Confidence: 0.7
- Marks deliberately-unused parameters with `del param` inside the function body (e.g. `del preview`, `del event`) rather than renaming them or leaving them silently unused. Confidence: 0.6
- New interaction tests reuse the shared `tests/conftest.py` helpers (e.g. `drag_on_canvas`) instead of re-inlining QMouseEvent synthesis — including handler-level tests that drive the real tool through synthesized events. Confidence: 0.7
- Keeps full type hints on every touched signature — parameters and return type (e.g. `_commit(...) -> bool`, `_apply_crop(rect: QRectF) -> bool`) — even on small edits. Confidence: 0.6
tained low-risk fixes, and — when the fix's blast radius exceeds the bug's severity (e.g. overriding `shape()` to fix crop retention would tighten selection hit-testing app-wide) — records an explicit "Accepted limitation (documented, not fixed)" with rationale in the phase doc rather than fixing or silently skipping. Cross-checks the reviewer's "NO ISSUE" verifications against own reading of the code too. Confidence: 0.85
- Every accepted finding that gets fixed is pinned by a named regression test exercising the failure mode (e.g. `test_apply_crop_clears_in_flight_resize_state`), counted in the suite delta and logged in the phase doc before the phase closes. Confidence: 0.8
- Prefers minimal-diff fixes that leave existing signatures and call sites untouched: when a bug can be fixed either by reshaping shared data structures (forcing projectors/callers to unpack new elements) or by a small additive change (a parallel `_resize_pos_before` captured alongside), picks the additive change and states the trade-off explicitly in the summary. Confidence: 0.75
- Centralizes gesture/lifecycle state teardown in one cancel helper (e.g. `_cancel_resize_tracking()` nulling all tracking fields) and routes every exit path through it — tool-mode change, crop/apply, commit — instead of nulling fields ad hoc at each site, so stale references can't survive into other operations. Confidence: 0.7
- Rates review findings on a Critical / Major / Minor severity scale. Confidence: 0.8
- Skips a review category entirely when it has no issues rather than writing a "no issues found" line for every section. Confidence: 0.8
- Does not assume intent that can't be verified from the code — flags it as a question instead of guessing. Confidence: 0.8
- Wants code reviews to report only problems — no praise and no "what the code does" summary. Confidence: 0.8
- Maintains a canonical per-phase history file (e.g. `upgrade.md`) as the single source of truth for project state, with a `Status` block at the top of `AGENTS.md`-style agent guides summarizing test counts, last-verified gate, and build artifact status. All other doc files are treated as derived state to be kept in sync, and are read-and-classified before any update. Confidence: 0.7
- When asked to update project status in docs, audits every `.md` file first (glob + read), classifies each as "needs update" / "already correct / pointer-only" / "historical artifact", edits only the stale ones, and leaves files that are pointers to the canonical doc untouched. Confidence: 0.7
- For historical phase-plan files (e.g. `phase11-plan.md`), adds a one-line `> Status: completed YYYY-MM-DD` header at the top that points to the canonical history file, and leaves the original implementation brief body intact — rather than rewriting the historical artifact. Confidence: 0.7
- After documentation-only edits, re-runs the full `pytest` suite as a sanity gate (treating "docs green + tests still green" as the close criterion), even though the changes don't touch code. Confidence: 0.65
- Does not comment on formatting, naming, or style in a review unless it causes a real bug. Confidence: 0.8
- Reviews code across five dimensions: correctness bugs, security issues (injection/auth/path/etc.), edge cases (empty/boundary/concurrent/large/malformed/timeout), error handling (swallowed exceptions, missing cleanup, unclear messages), and design/maintainability only when it risks real bugs later. Confidence: 0.8
- Verifies risky correctness claims empirically before asserting them in a review: writes a minimal scratchpad script and runs it headless (`QT_QPA_PLATFORM=offscreen`, `PYTHONPATH` to the repo) to confirm or refute the suspected bug (e.g. DPR preservation, dangling resize-layer references, handle-size scaling), then reports the finding as empirically confirmed or flags it as a question when it can't be verified from code alone. Confidence: 0.7
- When re-reviewing after a "done"/"all phases complete" signal, re-reads the code and re-runs the empirical checks to confirm previously reported findings were actually fixed (reporting them as "still present" when not), rather than trusting the completion signal. Confidence: 0.7
- When asked to verify fixes ("just check the code belongs to the issues"), re-reads the affected files and maps each reported issue to the exact file:line of its fix, marking each ✅ (or "still present") — not a broad "looks fixed". Confidence: 0.7
- When verifying fixes, also surfaces any changes the implementer made beyond the reported issues — especially ones that change observable behavior — and flags them as needing a conscious decision rather than silently accepting scope creep. Confidence: 0.7
- Prefers large refactors to be delivered in attributable stages, extracting the core behavior first, then integrating dependent adapters, then handling serialization and regression cleanup, so regressions can be localized. Confidence: 0.95
- Prefers domain/state modules to store framework-neutral records, keeping UI framework objects and view construction in an adapter so core behavior can be tested without a GUI view. Confidence: 0.95
- Prefers replacing private internal representations completely behind a stable interface when refactoring, preserving observable behavior rather than preserving tuple or data-layout compatibility that callers should not see. Confidence: 0.95
- Prefers split mutation ownership: the UI adapter performs framework-specific geometry and interaction mechanics, while the domain/history module owns state comparison, commit acceptance, history updates, and redo invalidation. Confidence: 0.95
- Prefers broad handler or window decoupling to be deferred until after the core seam is extracted and proven, avoiding multiple moving seams and keeping the first change independently testable. Confidence: 0.95
- Prefers framework-neutral records to be self-contained and explicitly discriminated by kind, with restoration creating fresh UI objects rather than persisting framework item identity. Confidence: 0.9
- Keeps transient presentation and gesture state—selection, text-edit mode, resize handles, and previews—out of document history and persistent domain state. Confidence: 0.9
- Prefers core domain/history modules to be signal-free; framework adapters own UI signals and translate accepted commits or restoration into view updates. Confidence: 0.9
- Prefers delivery order that establishes the framework-neutral domain seam first, integrates the UI adapter second, and adds serialization/regression coverage before tackling deferred coupling cleanup. Confidence: 0.9
- Delegates multi-question design decisions to the agent's recommended options, preferring the agent to apply all recommendations rather than requiring the user to choose each option individually. Confidence: 0.9
